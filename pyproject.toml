[project]
name = "video-downloader"
version = "0.2.0"
description = "Video downloader with pause/resume support"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "yt-dlp>=2026.3.13",
]
